import { UniversalFormDirective } from './universal-form.directive';

describe('UniversalFormDirective', () => {
  it('should create an instance', () => {
    const directive = new UniversalFormDirective();
    expect(directive).toBeTruthy();
  });
});
